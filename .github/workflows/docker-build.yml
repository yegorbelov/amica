name: Build & Push Backend

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-buildx-action@v4

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # --- Build & push main backend image ---
      - name: Build & push main backend image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          push: true
          platforms: linux/amd64
          tags: ghcr.io/${{ github.repository_owner }}/amica:latest
          cache-from: type=gha,scope=amica-backend
          cache-to: type=gha,mode=max,scope=amica-backend

      # --- Build & push nginx image ---
      - name: Build & push amica-nginx image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile.nginx
          push: true
          platforms: linux/amd64
          tags: ghcr.io/${{ github.repository_owner }}/amica-nginx:latest
          cache-from: type=gha,scope=amica-nginx
          cache-to: type=gha,mode=max,scope=amica-nginx
